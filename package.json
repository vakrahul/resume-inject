{
  "name": "resumeguard-ai",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "description": "ResumeGuard AI — Red/Blue prompt-injection lab (Node + Express + Nodemailer + Gemini + React)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w server && npm run build -w client",
    "vercel-build": "npm run build -w server && npm run build -w client",
    "start": "node server/dist/index.js",
    "test": "npm run test -w server",
    "lint": "npm run lint:ruff && npm run lint:node",
    "lint:ruff": "ruff check server/app tests harness",
    "lint:node": "npm test",
    "harness:red": "npm run harness:red -w server",
    "harness:blue": "npm run harness:blue -w server",
    "harness:all": "npm run harness:all -w server",
    "prepare": "husky"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "husky": "^9.1.7"
  }
}
